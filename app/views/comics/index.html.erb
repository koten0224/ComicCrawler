<h1>漫畫列表</h1>
<table class="table">
  <tr>
    <th>漫畫</th>
    <th>上次閱讀</th>
    <th>最新進度</th>
  </tr>
  <% @comics.each do |comic| %>
    <tr>
      <td><%= link_to comic.name, comic %></td>
      <td><%= comic.readed %></td>
        <td 
        <% if comic.latest_episode > comic.readed %>
          class="text-danger"
        <% end %>
        ><%= comic.latest_episode %></td>
      <td><button class="get-comic" comic-name="<%= comic.name %>" comic-id="<%= comic.id %>">追漫！</button></td>
      <td><button><%= link_to '取消追蹤', comic, method: :delete, data: {confirm: "你確定要刪除 #{comic.name} ㄇ？\n連你已爬取的記錄都會一併刪除歐！"} %></button></td>
    </tr>
  <% end %>

</table>
